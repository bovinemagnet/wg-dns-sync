site:
  title: wg-dns-sync
  start_page: wg-dns-sync::index.adoc

content:
  sources:
    - url: .
      branches: HEAD
      start_path: src/docs

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./build/site
